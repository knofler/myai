import { RouteSkeleton } from '@/components/boundary/route-skeleton';

export default function WelcomeStartLoading() {
  return <RouteSkeleton variant="form" maxWidth="max-w-2xl" titleWidth="w-36" />;
}
