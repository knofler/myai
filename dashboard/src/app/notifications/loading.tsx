import { RouteSkeleton } from '@/components/boundary/route-skeleton';

export default function NotificationsLoading() {
  return <RouteSkeleton variant="list" maxWidth="max-w-4xl" titleWidth="w-36" />;
}
