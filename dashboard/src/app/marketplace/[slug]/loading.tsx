import { RouteSkeleton } from '@/components/boundary/route-skeleton';

export default function MarketplaceItemLoading() {
  return <RouteSkeleton variant="detail" maxWidth="max-w-4xl" titleWidth="w-48" />;
}
