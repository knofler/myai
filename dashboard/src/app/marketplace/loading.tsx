import { RouteSkeleton } from '@/components/boundary/route-skeleton';

export default function MarketplaceLoading() {
  return <RouteSkeleton variant="grid" maxWidth="max-w-7xl" titleWidth="w-36" />;
}
