import { RouteSkeleton } from '@/components/boundary/route-skeleton';

export default function ConnectorsLoading() {
  return <RouteSkeleton variant="grid" maxWidth="max-w-5xl" titleWidth="w-32" />;
}
