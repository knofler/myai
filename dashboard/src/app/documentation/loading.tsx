import { RouteSkeleton } from '@/components/boundary/route-skeleton';

export default function DocumentationLoading() {
  return <RouteSkeleton variant="doc" maxWidth="max-w-5xl" titleWidth="w-40" />;
}
